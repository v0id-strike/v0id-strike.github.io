---
layout: layouts/base.njk
title: .voidrc
---

<section class="hero">
    <div class="container hero-content animate-fade-in">
        <h1 class="hero-title">Cyber Security Researcher & Developer</h1>
        <p class="hero-description">Exploring the depths of cybersecurity, sharing knowledge, and building secure systems.</p>
    </div>
</section>

<section id="notes" class="section">
    <div class="container">
        <h2 class="section-title">Security Notes</h2>
        <div class="tree-view">
            {% set posts = collections.posts %}
            {% set currentGroup = "" %}
            
            {% for post in posts %}
                {% set path = post.filePathStem | replace('/posts/', '') %}
                {% set pathParts = path.split('/') %}
                {% set group = pathParts[0] %}
                
                {% if group != currentGroup %}
                    {% if not loop.first %}
                        </div>
                    </div>
                    {% endif %}
                    <div class="tree-folder">
                        <div class="folder-header">
                            <span class="folder-icon">📁</span>
                            <span class="folder-name">{{ group }}</span>
                        </div>
                        <div class="tree-files">
                    {% set currentGroup = group %}
                {% endif %}
                
                <div class="tree-file">
                    <span class="file-icon">📄</span>
                    <a href="{{ post.url }}" class="file-link">{{ post.data.title }}</a>
                    {% if post.data.description %}
                        <span class="file-description">// {{ post.data.description }}</span>
                    {% endif %}
                </div>
                
                {% if loop.last %}
                    </div>
                </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</section>

<section id="projects" class="section">
    <div class="container">
        <h2 class="section-title">Featured Projects</h2>
        <div class="cards-grid">
            <div class="card animate-fade-in">
                <h3 class="card-title">Security Tools</h3>
                <p class="card-description">Collection of custom security tools and scripts for penetration testing and analysis.</p>
            </div>
            <div class="card animate-fade-in">
                <h3 class="card-title">Research Papers</h3>
                <p class="card-description">In-depth analysis and research on various cybersecurity topics and vulnerabilities.</p>
            </div>
            <div class="card animate-fade-in">
                <h3 class="card-title">CTF Writeups</h3>
                <p class="card-description">Detailed solutions and walkthroughs for various Capture The Flag challenges.</p>
            </div>
        </div>
    </div>
</section>

<section id="about" class="section">
    <div class="container">
        <h2 class="section-title">About Me</h2>
        <div class="card animate-fade-in">
            <p class="card-description">
                Cybersecurity researcher and developer passionate about exploring new technologies and security challenges.
                Focused on penetration testing, vulnerability research, and secure software development.
            </p>
        </div>
    </div>
</section>