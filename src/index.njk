---
layout: layouts/base.njk
title: Void-Strike Cyber Security
---

<div class="terminal-container">
  <div class="terminal-header">
    <div class="terminal-buttons">
      <span class="terminal-button red"></span>
      <span class="terminal-button yellow"></span>
      <span class="terminal-button green"></span>
    </div>
    <span class="terminal-title">notes-tree --view=structured</span>
  </div>
  
  <div class="terminal-body">
    <h2 class="glow-text">Cyber Security Notes</h2>
    
    {% set posts = collections.posts | reverse %}
    
    {# Display terminal-style tree #}
    <div class="tree-view">
      {% for post in posts %}
        {% if loop.first or post.data.categories[0] != posts[loop.index0 - 1].data.categories[0] %}
          {% if not loop.first %}
            </div>
          </div>
          {% endif %}
          <div class="tree-folder">
            <span class="folder-icon">📁</span>
            <span class="folder-name">{{ post.data.categories[0] if post.data.categories else "Uncategorized" }}</span>
            <div class="tree-files">
        {% endif %}
        
        <div class="tree-file">
          <span class="file-icon">📄</span>
          <a href="{{ post.url }}" class="file-link">{{ post.data.title }}</a>
          {% if post.data.description %}
            <span class="file-description">// {{ post.data.description }}</span>
          {% endif %}
        </div>
        
        {% if loop.last %}
          </div>
        </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</div>